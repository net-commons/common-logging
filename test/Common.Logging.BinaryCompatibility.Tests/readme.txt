This "CommonLoggingBinaryCompatibilityTest.exe" is compiled against the previous version of the Common.Logging assembly and
all methods within the ILog interface are called.

Execute "CommonLoggingBinaryCompatibilityTest.exe" against the new version
(adjust CommonLoggingBinaryCompatibilityTest.exe.config for version redirect) to ensure
the new version is binary compatible with the previous version.

